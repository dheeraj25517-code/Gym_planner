import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Backgrounds
  static const Color background = Color(0xFF090C0F);
  static const Color cardBackground = Color(0xFF12161A);
  static const Color cardStroke = Color(0xFF1E242B);

  // High Contrast Neon Accent Tones
  static const Color neonCyan = Color(0xFF00E5C1);
  static const Color neonOrange = Color(0xFFE57C00);
  static const Color neonYellow = Color(0xFFFFB800);

  // Text System Variations
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF6B7A87);
  static const Color textMuted = Color(0xFF404C56);

  // Specific Layout Control Buttons
  static const Color buttonDarkBg = Color(0xFF161B22);
  static const Color selectionActive = Color(0xFF1C2D35);
}